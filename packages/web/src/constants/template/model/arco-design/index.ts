import SelectOptionGroup from '@/constants/select-option-group';
import formTemplate from './form';
import tableTemplate from './table';

export default new SelectOptionGroup('arco-design', {
  id: 'fc231b1da9c0929f',
  options: [
    formTemplate,
    tableTemplate,
  ],
});
