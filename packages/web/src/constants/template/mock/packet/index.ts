import SelectOptionGroup from '@/constants/select-option-group';
import mockTemplate from './mock';
import mock2Template from './mock2';

export default new SelectOptionGroup('内置分组', {
  id: 'e3dw1fd4343f3cfe',
  options: [
    mockTemplate,
    mock2Template,
  ],
});
