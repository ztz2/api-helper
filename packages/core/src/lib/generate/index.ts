import { generateAllApi } from './generate-all-api';
import { generateClass, generateClassName } from './generate-class';
import { generateInterfaceName, generateInterface } from './generate-interface';
import { generateMap, generateMapComment, generateMapName } from './generate-map';
import { generateRequestFunctionName, generateRequestFunc } from './generate-request-func';

export {
  generateAllApi,
  generateClass,
  generateClassName,

  generateInterfaceName,
  generateInterface,

  generateMap,
  generateMapComment,
  generateMapName,

  generateRequestFunctionName,
  generateRequestFunc
};
